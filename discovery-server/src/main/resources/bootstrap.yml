spring:
  application:
    name: discovery-server
  cloud:
    config:
      enabled: true
      failFast: false
      retry:
        initialInterval: 3000
        multiplier: 1.3
        maxInterval: 5000
        maxAttempts: 20
      uri: http://localhost:8888
